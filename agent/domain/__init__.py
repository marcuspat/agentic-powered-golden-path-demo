"""Domain layer: aggregates, value objects, domain services, ports, events.

This layer depends on nothing else in the codebase. Infrastructure and
application layers depend on it; never the reverse.
"""
