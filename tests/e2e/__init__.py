"""Tier 3 — end-to-end tests against a live cluster and live GitHub.

Skipped unless ``RUN_E2E=1`` is exported. Runs nightly and on ``release/*``
branches in CI; never on PRs originating from forks.
"""
