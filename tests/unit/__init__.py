"""Tier 1 — pure-function unit tests.

These tests must run in milliseconds, never touch the network, never spawn
subprocesses, and never read or write files outside ``tmp_path`` fixtures.
They are the first signal in CI.
"""
