# Development & test dependencies for the Golden Path agent.
#
# Production runtime dependencies live in `requirements.txt` and are owned by
# the orchestrator agent slice; do not duplicate them here.
#
# Pinning is `~=` (compatible release) so security patches flow in without
# breaking on a major version bump.

# --- Test runner & plugins -------------------------------------------------
pytest~=8.3
pytest-cov~=5.0
pytest-mock~=3.14
pytest-benchmark~=4.0

# --- Lint & format ---------------------------------------------------------
ruff~=0.6

# --- Type checking ---------------------------------------------------------
mypy~=1.11

# --- Security: dependency CVE scanner --------------------------------------
pip-audit~=2.7
